const STATUS = {
    FUNDRAISING: 1,
    ACTIVE: 2,
    CLOSED: 3,
};

const APPROVAL = {
    INITIAL: 0,
    ACCEPTED: 1,
    REJECTED: 2
}

const SYNC = {
    INITIAL: 0,
    TO_SYNC: 1,
    SYNCED: 2
};

export { STATUS, SYNC, APPROVAL };
